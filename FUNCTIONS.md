# 函数说明

主源码是 `v3_optimized.js`，油猴版复制同一实现。以下列出全部命名函数/方法，源码内也有对应中文注释。

## 启动与资源识别

| 函数 | 作用 |
| --- | --- |
| `bootstrap` | 区分测试和浏览器；只在顶层运行，清理已有实例并启动新助手 |
| `createPlayer` | 创建配置、状态和控制器；测试时可注入可控时钟 |
| `text` | 清理标签空白与编号，统一步骤标题 |
| `visible` | 排除隐藏元素及祖先，不排除屏幕下方尚未滚到的内容 |
| `frameDocument` | 读取同源 iframe 的 document，访问失败返回 null |
| `videoFrame` | 优先用视频专用类别或模块路径识别播放器，避免通用附件类别混淆 |
| `documentFrame` | 排除视频后识别候选文档 iframe，不记录完整资源网址 |
| `resourceFrame` | 识别课程和视频框架，避免跳过未加载资源 |
| `taskWrapper` | 向上寻找任务点外壳，跨过同源 iframe 边界 |
| `taskPointState` | 读取单个任务点的三态结果：已确认、未完成、无可靠标记；不借用嵌套任务点状态 |
| `fingerprint` | 用资源类型和地址作为内存内身份，防止文档完成记录误用于待加载视频 |
| `resourceDocument` | 取得媒体所属文档或 iframe 当前内容文档，区分外壳相同而内容已换页的情况 |
| `courseContext` | 用 URL 解析已加载课程文档的小节编号和卡片编号，只保留这两个值，不输出完整地址 |
| `navigationSnapshot` | 固定切换前的资源、播放结束状态和课程框架文档引用，作为新旧内容的比较依据 |
| `scanResources` | 顺序扫描资源和课程框架；实际视频优先，未初始化播放器保留为待加载 |
| `scanResources.visit` | 有界递归扫描同源页面并去重 |
| `blocker` | 检查可见验证/登录/错误弹窗，返回通用提示 |

## 面板与运行控制

| 方法 | 作用 |
| --- | --- |
| `_mountPanel` | 创建隔离样式的可收起面板；停止和收起按钮留在标题栏 |
| `setCollapsed` | 只改变面板显示、宽度与无障碍状态，不改变播放、计时器或静音选项 |
| `setMuted` | 更新本页静音选项和复选框，立即控制当前视频，后续视频沿用；不触发播放 |
| `_status` | 更新状态、标题悬停提示和按钮；收起后仍可查看状态，只在状态变化时输出通用日志 |
| `run` | 启动新一轮检查，取消旧轮询和过期异步结果 |
| `stop` | 停止轮询和受控媒体，恢复原有静音/倍速 |
| `_releaseActive` | 解除媒体事件、暂停当前视频并恢复其原来的静音和倍速 |
| `destroy` | 清理面板、轮询及页面事件监听 |
| `tick` | 按稳定小节/卡片编号同步页面，目录重绘不暂停；核对整节完成后处理资源，矛盾状态不跳过 |
| `_completeTask` | 记录已浏览或平台确认的资源，保留自然结束时刻以免叠加整页等待 |
| `_pageTaskStates` | 收集本页全部可见任务点，包括尚未识别成视频/文档的任务，避免漏项 |
| `_skipConfirmedResources` | 跳过已确认资源；仅部分任务达标时，当前正在播放的视频仍等自然结束 |

## 小节与步骤导航

| 方法 | 作用 |
| --- | --- |
| `_nodes` | 每次重新读取目录，避免持有过期对象 |
| `_currentNode` | 仅在唯一激活节点存在时确认当前位置 |
| `_nodeKey` | 用 cur 编号或元素 ID 识别小节，无 ID 才回退到目录位置与名称；不依赖 DOM 对象引用 |
| `_loadedUnitMatches` | 已加载课程地址提供小节编号时，核对它与目录目标一致，拒绝接管错误内容 |
| `_unitComplete` | 核对当前目录节点的明确完成图标，排除正数待完成计数和嵌套节点标记 |
| `_unitComplete.owned` | 只收集所属目录节点相同的标记，防止父子或兄弟节点串用状态 |
| `_steps` | 获取顶部步骤顺序，排除嵌套重复项 |
| `_currentStepTitle` | 读取当前步骤标题，识别媒体、学习目标与测验 |
| `_currentStepKey` | 优先读已加载文档的卡片编号；回退只读已知步骤标签的直接文字，排除章节标题与提示子节点变化 |
| `_navigate` | 保存旧资源快照与目标稳定编号，拒绝重复编号，原生点击下一步骤/小节 |
| `_awaitNavigation` | 核对目标编号与旧资源替换；认可新文档或实际小节/卡片编号变化，兼容局部换页；空旧页、无关框架加载或归属冲突不能证明就绪，超时停止 |
| `nextUnit` | 跳过连续明确完成的目录项，一次点击首个未完成小节，拒绝猜测当前位置 |
| `_advance` | 全部资源结束后优先走本节下一步骤；遇到测验停止 |

## 视频与文档

| 方法 | 作用 |
| --- | --- |
| `_startVideo` | 保存原设置，在播放前应用配置倍速（默认 2 倍）及勾选的静音，绑定媒体事件 |
| `_attemptPlayback` | 调用原生 play，处理静音、初始化重试和过期结果 |
| `_videoTick` | 自然结束后每轮检查保存，确认即继续；已知未完成超时停止，无标记才保守等待 |
| `_documentViews` | 获取文档内部同源页面，报告跨域不可访问情况 |
| `_documentViews.visit` | 有界递归收集阅读页面并去重 |
| `_pagination` | 获取可靠当前页、总页数和下一页按钮 |
| `_scrollArea` | 找实际可滚动阅读区或文档根节点 |
| `_documentTick` | 默认约每 250 毫秒翻页/滚动，核对实际变化；末尾独立等待至少 1 秒，失败停止 |

## 回调与测试辅助

- `_onStart`、`_onStop`、`_onUnload` 分别绑定 `run`、`stop`、`destroy`，让事件可以正常清理。
- `_onMuteChange` 把静音复选框的用户选择交给 `setMuted`；自动播放静音回退也调用 `setMuted`，避免实际静音与面板勾选不同步。
- `_onCollapse` 只切换 `setCollapsed` 的显示状态，不调用开始/停止逻辑。
- `active.onEnded` 只登记自然结束时刻；`active.onError` 停止故障媒体。Promise 回调会核对所属运行是否仍有效。
- 数组回调只做查找/过滤/比较，轮询只调用 `tick`；没有未登记的递归延时。

| 测试函数 | 作用 |
| --- | --- |
| `flush` | 等待 Promise 回调结算 |
| `harness` | 创建 jsdom、假时钟和轮询表，登记清理 |
| `harness.start` / `harness.advance` | 启动 / 推进模拟时钟并检查一次 |
| `frame` | 创建合成同源 iframe 和已加载状态 |
| `denyFrame` | 模拟跨域 iframe 的空 document 与访问异常 |
| `media` | 创建合成视频和媒体状态 |
| `media.play` / `media.pause` / `media.end` | 模拟播放、暂停和自然结束 |
| `pagedDocument` | 创建页码、总页数与原生翻页按钮 |
| `scrollDocument` | 创建可控高度的滚动文档与懒加载模型 |
| `taskPoint` / `taskPoint.setComplete` | 创建合成任务点外壳，在测试中模拟平台完成状态的变化 |
| `completeUnit` | 在测试中添加目录的明确完成标记，不根据播放进度推算 |
| `coursePage` | 创建带合成小节/卡片编号的课程框架，用于验证目标内容和导航同步；不请求真实课程 |

本地浏览器测试页面的函数：`record` 显示观察事件，`makeClip` 用 canvas/MediaRecorder 生成真实短视频，内部 `draw` 绘制动画帧，`addVideo` 创建原生播放器，`setupFixture` 按“视频一、三页文档、视频二、下一节”布置验证页面。所有数据均为合成内容，不访问账号或课程服务。

构建器和 `verify-v3.mjs` 使用模块顶层代码，没有额外函数，分别生成安装入口与检查语法/同步。每个测试回调的前置注释说明其验证目标。
